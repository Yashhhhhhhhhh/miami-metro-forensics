## 2024-05-24 - Missing Accessible Names on Inputs and Buttons
**Learning:** Native HTML input sliders (`<input type="range">`) and icon-only buttons often lack accessible names by default, leading to poor screen reader experiences.
**Action:** Always verify that every interactive element has an accessible name, either via visible text, an associated `<label>` tag, or an explicit `aria-label` attribute.
